extends Node

signal entity_placed(entity, cell)

signal entity_remove(entity, cell)

signal systems_ticked(delta)

signal entered_picked_area(entity, player)

signal hover_over_entity(entity)

signal info_updated(entity)
